export class ValidationError extends Error {
    constructor(issues) {
        super(issues[0].message +
            (issues[0].location ? `\n  at ${issues[0].location}` : ''));
        this.issues = [];
        this.issues = issues;
    }
}
